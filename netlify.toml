[build]
  command = "npm run build"
  publish = ".next"
  node_version = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Explicitly route Next.js API endpoints to the generated functions
[[redirects]]
  from = "/api/*"
  to   = "/.netlify/functions/next_api_:splat"
  status = 200
