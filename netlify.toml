[build]
  command = "npm run build"
  publish = ".next"
  node_version = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Rewrite /api/* to Next.js internal handler (not a redirect)
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
  force = true
